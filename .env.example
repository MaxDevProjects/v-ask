# ========================================
# Environnement (docker compose)
# ========================================
#
# Ne commitez jamais vos vrais secrets.
# Utilisation recommandée :
# - Local : créez `.env.dev` à partir de ce fichier
# - Prod  : fournissez `.env.prod` sur le VPS

# ----------------------------
# Exposition des conteneurs
# ----------------------------
FRONTEND_HOST_PORT=3100
BACKEND_HOST_PORT=3101

# Ports internes (conteneur)
BACKEND_PORT=3001

# Base URL publique de l'API (utilisée par Nuxt runtimeConfig)
# DEV  : http://localhost:3101
# PROD : http://<IP_OU_DOMAINE>:3001 (ou https://...)
NUXT_PUBLIC_API_BASE=http://localhost:3101

# CORS (origines autorisées, séparées par des virgules)
# DEV  : http://localhost:3100
# PROD : http://<IP_OU_DOMAINE> (ou https://...)
CORS_ORIGIN=http://localhost:3100

# ----------------------------
# Auth / sécurité
# ----------------------------
# IMPORTANT : en prod, mettez une valeur longue et aléatoire.
JWT_SECRET=change-me-in-prod

# ----------------------------
# AI Provider Configuration
# ----------------------------
# Priority: GEMINI_API_KEY > OPENAI_API_KEY

# Google Gemini (recommended)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI (alternative)
# OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# Model override (optional)
AI_MODEL=gemini-2.5-flash
