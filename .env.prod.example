# Production (VPS)
#
# Remplissez PUBLIC_HOST avec l'IP publique ou un nom de domaine (recommandé).
PUBLIC_HOST=203.0.113.10

# Exposition publique
FRONTEND_HOST_PORT=3100
BACKEND_HOST_PORT=3101
BACKEND_PORT=3001
PWA_DEV=false
APP_TIMEZONE=Europe/Paris

# Front -> API (depuis le navigateur)
NUXT_PUBLIC_API_BASE=http://${PUBLIC_HOST}:3101

# CORS côté backend (origine du frontend public)
CORS_ORIGIN=http://${PUBLIC_HOST}

# Secrets (obligatoire en prod)
JWT_SECRET=replace-with-long-random-secret

# AI (au choix)
GEMINI_API_KEY=
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gemini-2.5-flash
